function counter() 

{
    // Write your code below. Hint: you can finish it in about 3-5 lines.

    // create a variable to hold the p tag we want to change the text of

    // Convert the string of the p tag into a integer so you can add to it

    // Update the text of the p tag with new value

    }
    counter_number= document.getElementById(counter_number)
    counter_nmber.innerHTML=parseInt(counter_number.innerHTML)
